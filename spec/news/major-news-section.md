# 홈페이지 주요 뉴스 섹션 기능 명세서

## 📋 기능 개요

홈페이지에 국내 증시 주요 뉴스 섹션을 추가하여 투자자가 최근 시장 동향을 빠르게 파악할 수 있게 합니다.

### 목적

- 투자자에게 어제~오늘 주요 증시/경제 뉴스를 한눈에 제공
- 홈페이지의 정보 가치 향상
- 기존 Google News RSS 인프라를 재활용한 효율적 구현

### 배치

- 현재: ServiceBanner → PopularCompaniesSection → TodayDisclosures
- 변경: ServiceBanner → PopularCompaniesSection → **MajorNewsSection** → TodayDisclosures

---

## 🎯 요구사항

### 1. 검색 키워드 (테마별)

5개 테마 키워드로 Google News RSS 병렬 조회 → 병합 → 중복제거:

| 키워드   | 커버리지             |
| -------- | -------------------- |
| `코스피` | KOSPI 지수 관련 뉴스 |
| `코스닥` | KOSDAQ 관련 뉴스     |
| `증시`   | 증시 전반 뉴스       |
| `금리`   | 금리/통화정책 뉴스   |
| `환율`   | 환율/외환시장 뉴스   |

각 키워드에 `when:2d` 파라미터를 추가하여 Google News RSS 서버 측에서도 최근 2일 뉴스만 조회합니다.

### 2. 뉴스 개수

- **표시 개수**: 6개
- **레이아웃**: 2열 그리드 × 3행 (PC), 1열 × 6행 (모바일)

### 3. 기간 필터

- **어제~오늘 (48시간)** 뉴스만 표시
- Google News `when:2d` + 클라이언트 측 날짜 필터 이중 적용

### 4. 표시 정보

- 뉴스 제목 (2줄 말줄임)
- 매체명 (예: 한국경제, 매일경제)
- 게시 시간 (상대 시간: "5분 전", "2시간 전")

### 5. 인터랙션

- 뉴스 클릭 → 원문 링크 새 탭 열기 (`target="_blank"`)
- 로딩 → 스켈레톤 UI
- 에러 → 에러 폴백 + 재시도 버튼
- 빈 목록 → "최근 뉴스가 없습니다" 메시지

---

## 🎨 UI/UX 상세

### PC 버전 (≥768px)

```
┌──────────────────────────────────────────────────────────────────────┐
│  주요 뉴스                                                           │
│  최근 24시간 주요 증시 뉴스                                            │
├──────────────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────┐  ┌──────────────────────────┐         │
│  │ 코스피, 외국인 매수에     │  │ 한은 기준금리 동결...     │         │
│  │ 2700 돌파                 │  │ 하반기 인하 가능성        │         │
│  │ 한국경제 · 1시간 전       │  │ 조선비즈 · 3시간 전       │         │
│  └──────────────────────────┘  └──────────────────────────┘         │
│  ┌──────────────────────────┐  ┌──────────────────────────┐         │
│  │ 반도체 수출 역대 최대...  │  │ 원/달러 환율 1,300원대    │         │
│  │ 삼성·SK 수혜              │  │ 진입 전망                 │         │
│  │ 매일경제 · 5시간 전       │  │ 서울경제 · 6시간 전       │         │
│  └──────────────────────────┘  └──────────────────────────┘         │
│  ┌──────────────────────────┐  ┌──────────────────────────┐         │
│  │ 코스닥, 바이오 강세에     │  │ 미국 CPI 둔화...         │         │
│  │ 800선 회복                │  │ 글로벌 증시 반등          │         │
│  │ 파이낸셜뉴스 · 8시간 전   │  │ 이데일리 · 10시간 전      │         │
│  └──────────────────────────┘  └──────────────────────────┘         │
└──────────────────────────────────────────────────────────────────────┘
```

### 모바일 버전 (<768px)

```
┌─────────────────────────────────────────┐
│  주요 뉴스                               │
│  최근 24시간 주요 증시 뉴스               │
├─────────────────────────────────────────┤
│  ┌─────────────────────────────────────┐│
│  │ 코스피, 외국인 매수에 2700 돌파      ││
│  │ 한국경제 · 1시간 전                  ││
│  └─────────────────────────────────────┘│
│  ┌─────────────────────────────────────┐│
│  │ 한은 기준금리 동결... 하반기 인하     ││
│  │ 조선비즈 · 3시간 전                  ││
│  └─────────────────────────────────────┘│
│  ...                                    │
└─────────────────────────────────────────┘
```

---

## 🔧 기술 구현 방안

### 아키텍처 (FSD)

```
apps/web/
├── app/
│   ├── page.tsx                                  # [수정] 프리패치 + 위젯 추가
│   └── api/
│       └── news/
│           └── major/
│               └── route.ts                      # [신규] 주요 뉴스 API Route
│
├── entities/
│   └── news/
│       ├── index.ts                              # [수정] 클라이언트 export 추가
│       ├── server.ts                             # [수정] 서버 export 추가
│       ├── api/
│       │   └── google-news/
│       │       ├── server.ts                     # [수정] getMajorMarketNews 추가
│       │       └── client.ts                     # [수정] getMajorMarketNews 추가
│       ├── lib/
│       │   └── format-news.ts                    # [수정] deduplicateNews, filterNewsByDate 추가
│       └── queries/
│           ├── hooks.ts                          # [수정] useMajorMarketNews 추가
│           ├── prefetch.ts                       # [신규] prefetchMajorMarketNews
│           └── index.ts                          # [수정] export 추가
│
├── widgets/
│   └── major-news-section/                       # [신규] 전체 디렉토리
│       ├── index.ts
│       ├── major-news-section.tsx
│       └── ui/
│           ├── index.ts
│           ├── major-news-content.tsx
│           ├── major-news-card.tsx
│           └── major-news-skeleton.tsx
│
└── shared/
    └── lib/
        └── query-keys.ts                         # [수정] news.market 키 추가
```

### 데이터 흐름

```
[서버 사이드 - 페이지 로드]
page.tsx
  → prefetchMajorMarketNews(6)
    → getMajorMarketNews(6, 48) [server.ts]
      → Promise.allSettled([
          getGoogleNews('코스피 when:2d', 20),
          getGoogleNews('코스닥 when:2d', 20),
          getGoogleNews('증시 when:2d', 20),
          getGoogleNews('금리 when:2d', 20),
          getGoogleNews('환율 when:2d', 20),
        ])
      → 병합 → deduplicateNews() → filterNewsByDate(48h)
      → 최신순 정렬 → slice(0, 6)
    → dehydrate → HydrationBoundary

[클라이언트 사이드]
MajorNewsSection
  → Suspense + ErrorBoundary
    → MajorNewsContent
      → useMajorMarketNews(6)
        → 서버 프리패치 데이터로 즉시 렌더 (hydration)
      → MajorNewsCard × 6
```

### 캐싱 전략

| 대상            | 레이어        | 캐시 시간 | 설명                    |
| --------------- | ------------- | --------- | ----------------------- |
| Google News RSS | Next.js fetch | 5분       | RSS 개별 쿼리 서버 캐싱 |
| 주요 뉴스 응답  | React Query   | 5분       | 클라이언트 staleTime    |

---

## 🚀 구현 단계

### Step 1: 뉴스 유틸리티 함수 추가

**수정**: `entities/news/lib/format-news.ts`

```typescript
/**
 * 뉴스 목록에서 중복을 제거합니다 (link 기준)
 */
export function deduplicateNews(items: NewsItem[]): NewsItem[]

/**
 * 특정 기간 내의 뉴스만 필터링합니다
 */
export function filterNewsByDate(items: NewsItem[], hours?: number): NewsItem[]
```

### Step 2: 서버 API 함수 추가

**수정**: `entities/news/api/google-news/server.ts`

```typescript
/**
 * [서버 전용] 한국 주식 시장의 주요 뉴스를 조회합니다
 * 5개 테마 키워드로 검색 → 병합 → 중복제거 → 날짜필터 → 정렬
 */
export async function getMajorMarketNews(limit?: number, hoursBack?: number): Promise<NewsResponse>
```

### Step 3: 클라이언트 API 함수 추가

**수정**: `entities/news/api/google-news/client.ts`

```typescript
/**
 * [클라이언트 전용] /api/news/major 호출
 */
export async function getMajorMarketNews(limit?: number): Promise<NewsResponse>
```

### Step 4: API Route 생성

**신규**: `app/api/news/major/route.ts`

- GET `/api/news/major?limit=6`
- 서버 `getMajorMarketNews()` 호출

### Step 5: Query Key 추가

**수정**: `shared/lib/query-keys.ts`

```typescript
news: {
  company: (corpName: string) => [corpName],
  market: () => [],   // ← 추가
}
```

### Step 6: Query Hook 추가

**수정**: `entities/news/queries/hooks.ts`

```typescript
export function useMajorMarketNews(limit?: number)
// useSuspenseQuery, staleTime: 5분
```

### Step 7: Prefetch 함수 생성

**신규**: `entities/news/queries/prefetch.ts`

```typescript
export async function prefetchMajorMarketNews(limit?: number)
// 기존 disclosure/queries/prefetch.ts 패턴 동일
```

### Step 8: Entity Export 업데이트

- **수정**: `entities/news/index.ts` — 클라이언트 export 추가
- **수정**: `entities/news/server.ts` — 서버 export 추가
- **수정**: `entities/news/queries/index.ts` — hook export 추가

### Step 9: 위젯 생성

**신규**: `widgets/major-news-section/` 전체 디렉토리

- `major-news-section.tsx` — ErrorBoundary + Suspense 래퍼 (PC/모바일 반응형)
- `ui/major-news-content.tsx` — useMajorMarketNews 호출 + 리스트 렌더
- `ui/major-news-card.tsx` — 개별 뉴스 카드
- `ui/major-news-skeleton.tsx` — 로딩 스켈레톤

패턴 참조: `widgets/popular-companies-section/`

### Step 10: 홈페이지 통합

**수정**: `app/page.tsx`

- `prefetchMajorMarketNews(6)`을 `Promise.allSettled`에 추가
- `<MajorNewsSection />`을 PopularCompaniesSection과 TodayDisclosures 사이에 배치

---

## 📁 파일 목록

| 구분 | 파일 경로                                               |
| ---- | ------------------------------------------------------- |
| 수정 | `entities/news/lib/format-news.ts`                      |
| 수정 | `entities/news/api/google-news/server.ts`               |
| 수정 | `entities/news/api/google-news/client.ts`               |
| 수정 | `entities/news/queries/hooks.ts`                        |
| 수정 | `entities/news/queries/index.ts`                        |
| 수정 | `entities/news/index.ts`                                |
| 수정 | `entities/news/server.ts`                               |
| 수정 | `shared/lib/query-keys.ts`                              |
| 수정 | `app/page.tsx`                                          |
| 신규 | `app/api/news/major/route.ts`                           |
| 신규 | `entities/news/queries/prefetch.ts`                     |
| 신규 | `widgets/major-news-section/index.ts`                   |
| 신규 | `widgets/major-news-section/major-news-section.tsx`     |
| 신규 | `widgets/major-news-section/ui/index.ts`                |
| 신규 | `widgets/major-news-section/ui/major-news-content.tsx`  |
| 신규 | `widgets/major-news-section/ui/major-news-card.tsx`     |
| 신규 | `widgets/major-news-section/ui/major-news-skeleton.tsx` |

## 재사용 기존 코드

- `getGoogleNews()` (`entities/news/api/google-news/server.ts`) — RSS 조회
- `formatNewsItem()` (`entities/news/lib/format-news.ts`) — RSS → NewsItem 변환
- `formatRelativeTime()` (`entities/news/lib/format-news.ts`) — 상대 시간 포맷
- `getBaseUrl()` (`shared/lib/get-base-url.ts`) — 클라이언트 API 호출
- `queries` (`shared/lib/query-keys.ts`) — 쿼리 키 팩토리
- `ErrorBoundary` (`@suspensive/react`) — 에러 처리
- `Skeleton` (`@gs/ui`) — 로딩 UI

---

## ✅ 검증

1. `pnpm dev`로 개발서버 실행
2. 홈페이지(`/`) 접속 → 주요 뉴스 섹션 렌더링 확인
3. 뉴스 카드 클릭 → Google News 원문 새 탭 열림 확인
4. 모바일(1열)/PC(2열) 반응형 레이아웃 확인
5. 뉴스 API 실패 시 ErrorBoundary 동작 확인
6. `pnpm build` 빌드 성공 확인
